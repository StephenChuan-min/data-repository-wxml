<input
    class="native-input"
    confirm-type="{{confirmType}}"
    bindconfirm="doConfirm"
    bindinput="handleInput"
    bindfocus="handleFocus"
    bindblur="handleBlur"
    placeholder="请输入账号或姓名"
    focus="{{autoFocus}}"
    value="{{value}}"
/>